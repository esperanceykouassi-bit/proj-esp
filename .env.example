# Copier ce fichier en .env dans chaque sous-dossier (frontend / backend)
# Voir backend/.env.example pour les variables backend

# Frontend — aucune variable sensible requise pour démarrer
VITE_API_URL=http://localhost:3001/api
